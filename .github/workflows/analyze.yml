name: Analyze

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  # setup:
  #   uses: ./.github/workflows/setup.yml
  setup:
    name: Run setup
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.x"
          channel: "stable"
          cache: true

      - name: Setup melos
        uses: bluefireteam/melos-action@v3

  analyze:
    name: Run flutter analyze
    runs-on: ubuntu-latest
    #needs: setup

    steps:
      # - name: Restore PATH
      #   run: |
      #     echo "${{ needs.setup.outputs.github_path }}" >> $GITHUB_PATH

      # - name: Show path
      #   run: echo $PATH

      - name: Run analyze on all packages
        run: |
          echo "PWD: "
          pwd
          echo "ls: "
          ls .
      #    cd apps/reference
      #    flutter analyze
      #    echo $PATH
