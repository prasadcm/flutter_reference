name: referencee
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.0.0
  equatable: ^2.0.3
  cmp_widgets:
    path: ../../packages/cmp_widgets
  cmp_search_suggestion:
    path: ../../packages/cmp_search_suggestion

  go_router: ^14.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  mockito: ^5.0.0

flutter:
  uses-material-design: true
