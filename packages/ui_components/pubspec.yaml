name: ui_components
description: "Flutter package for reusable UI components."
version: 0.0.1
homepage:
publish_to: "none"

environment:
  sdk: ^3.6.1
  flutter: ">=1.17.0"

dependencies:
  cached_network_image: ^3.4.1
  core:
    path: ../../packages/core
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
  get_it: ^8.0.3
  mocktail: ^1.0.4
  remove_from_coverage: ^2.0.0
  very_good_analysis: ^7.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/placeholder/
